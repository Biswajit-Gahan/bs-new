import styled from "styled-components";

export const MapSectionContainer = styled.div`
  height: inherit;
  min-height: inherit;

  .leaflet-container {
    height: 100%;
    min-height: 100%;
  }
`;